# Graph Visualization Model

`core/correlation/graph.py` takes the relationships table and generates a generic JSON `nodes` and `links` structure.
This payload is compatible natively with **D3.js** and **Vis.js** for front-end integration.
