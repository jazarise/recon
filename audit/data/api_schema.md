# API Schema

All API routes now read directly from the `core/database/` layer rather than calling plugins directly.

Routes:
- `GET /targets`
- `GET /hosts`
- `GET /findings`
- `GET /vulnerabilities`
- `GET /scans`
