name: subdomains_recon88r
version: 1.0.0
description: "Native subdomain enumeration (ported from Recon88r)"
author: ReconX
type: discovery
stage: deep

execution:
  handler: adapter.ToolAdapter

config:
  timeout: 3600
