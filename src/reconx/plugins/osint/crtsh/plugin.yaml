name: osint_crtsh
version: 1.0.0
description: "Native Certificate Transparency log parsing (ported from recon-ng)"
author: ReconX
type: reconnaissance
stage: discovery

execution:
  handler: adapter.ToolAdapter

config:
  timeout: 15
