name: ssl_analysis
version: 1.0.0
description: "Native SSL certificate analysis (ported from FinalRecon)"
author: ReconX
type: intelligence
stage: discovery

execution:
  handler: adapter.ToolAdapter

config:
  timeout: 10
