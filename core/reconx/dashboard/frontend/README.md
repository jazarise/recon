# ReconX Dashboard Frontend

This directory is reserved for the React/Vite/Tailwind frontend.

As per the migration strategy:
"Add frontend after backend stabilizes"

To initialize the frontend later:
```bash
npm create vite@latest . -- --template react-ts
npm install
npm run dev
```
