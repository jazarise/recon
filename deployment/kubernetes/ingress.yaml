apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: reconx-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: reconx.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: reconx-service
            port:
              number: 80
