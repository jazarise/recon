name: scan4all_vuln
version: 1.0.0
description: "Native vulnerability scanning orchestration (ported from scan4all)"
author: ReconX
type: vulnerability
stage: deep

execution:
  handler: adapter.ToolAdapter

config:
  timeout: 14400
