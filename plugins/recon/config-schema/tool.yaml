name: config-schema
category: recon
language: .json
execution_type: library
entrypoint: config-schema.json
wrapper: adapter.py
input_type:
  - domain
  - url
output_type:
  - findings
dependencies:
  - config-schema
supports_parallel: true
supports_distributed: true
risk_level: medium
