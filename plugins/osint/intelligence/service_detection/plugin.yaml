name: scan4all_service_detection
version: 1.0.0
description: "Native service detection (ported from scan4all)"
author: ReconX
type: intelligence
stage: deep

execution:
  handler: adapter.ToolAdapter

config:
  timeout: 3600
