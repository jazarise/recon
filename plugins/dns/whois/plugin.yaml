name: dns_whois
version: 1.0.0
description: "Native Python WHOIS querying using direct socket connections (from FinalRecon)"
author: ReconX
type: reconnaissance
stage: discovery

execution:
  handler: adapter.ToolAdapter

config:
  timeout: 10
