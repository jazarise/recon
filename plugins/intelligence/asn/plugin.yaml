name: asn_intel
version: 1.0.0
description: "Native ASN intelligence using BGPView (ported from metabigor)"
author: ReconX
type: reconnaissance
stage: discovery

execution:
  handler: adapter.ToolAdapter

config:
  timeout: 10
