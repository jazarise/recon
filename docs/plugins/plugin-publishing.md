# Plugin Publishing

Currently, ReconX does not have a central Plugin Registry.

To share a plugin with the community:
1. Create a public GitHub repository.
2. Structure it as a standard Python module containing `plugin.py`.
3. Users can clone it directly into their `src/reconx/plugins/` directory.

We plan to introduce a formal `ReconX Marketplace` in v2.0.
