# ReconX V2.0.0 Operations Guide
## Installation
Use Docker Compose for production deployments.
## Maintenance
Run `reconx health` to monitor system stability.
Run `reconx backup` periodically to archive SQLite databases.
